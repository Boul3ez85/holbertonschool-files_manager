import redisClient from '../utils/redis.js';
const jwt = require('jsonwebtoken');


export default class AuthController {
  async static getConnect() {
    const userEmail = req.body.email;
    const userPassword = req.body.password;

  }
}
